# frozen_string_literal: true

source "https://rubygems.org"

# GitHub Pages meta-gem: pins Jekyll + plugins + supported themes to match GitHub's build.
# Your theme is set in _config.yml (theme: jekyll-theme-architect) and is included here.
gem "github-pages", group: :jekyll_plugins
